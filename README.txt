v0.2.1 timeout fix

1. Copy app/ and lib/ into your existing stock-opportunity-scanner folder.
2. Choose Replace when Windows asks.
3. Run:
   git add .
   git commit -m "Fix Vercel scan timeout"
   git push
4. Vercel will redeploy automatically.
